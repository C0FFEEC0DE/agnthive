{
  "id": "subagent-architect-refactor-lite",
  "suite": "subagents_smoke",
  "agent_alias": "a",
  "category": "refactor",
  "related_agents": ["a"],
  "fixture": "text-report",
  "review_required": false,
  "docs_required": false,
  "verification_required": true,
  "prompt": "Use @a to extract the duplicated strip+uppercase normalization from render_title() and render_subtitle() into a shared helper. Do not edit any file yourself — only @a touches the code. @a must run pytest -q and confirm 4 passed. Finish with required footer.",
  "success_criteria": [
    "Duplication reduced",
    "Behavior unchanged",
    "pytest -q passes"
  ],
  "must_not": [
    "Change output format",
    "Skip pytest -q",
    "Perform release or deploy work"
  ],
  "forbidden_transcript_patterns": [
    "footer repair",
    "shell guard"
  ],
  "required_transcript_patterns": [
    "Outcome:",
    "Changed files:|No files changed:",
    "Verification status:",
    "Remaining risks:|Next step:"
  ],
  "required_used_agents": ["a"],
  "dispatch_contract": {
    "mode": "enforced",
    "required_agents": ["a"],
    "root_only": true
  }
}
